name: CI - Feature Branch

on:
  push:
    branches: [feature/**]

jobs:
  quick-lint:
    name: Quick lint
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18.1-alpine
        env:
          POSTGRES_USER: ci_test
          POSTGRES_PASSWORD: ci_test_password
          POSTGRES_DB: nestjs_demo_basic_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DB_URL: postgresql://ci_test:ci_test_password@localhost:5432/nestjs_demo_basic_test?schema=public
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          # 若远程仓库中存在依赖锁文件，且需要缓存依赖以加速构建过程，可取消下一行注释
          # cache: 'pnpm'

      - name: Install dependencies
        run: |
          echo "📥 安装项目依赖"
          pnpm install
          echo "✓ 依赖安装完成"

      - name: Extract version from package.json
        id: version
        run: |
          VERSION=$(node -e "console.log(require('./package.json').version)")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Application Version: $VERSION"

      - name: Format & Lint code
        run: |
          echo "📝 格式化代码"
          pnpm prettier --check .
          echo "✓ 格式化完成"
          echo "✅ 质量检查"
          pnpm eslint
          echo "✓ 质量检查完成"

  finished:
    name: Finished
    needs: quick-lint
    if: always()
    runs-on: ubuntu-latest
    env:
      COMMIT_MESSAGE: ${{ github.event.head_commit.message }}

    steps:
      - name: Generate summary
        run: |
          TIMESTAMP_UTC=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          TIMESTAMP_CN=$(TZ='Asia/Shanghai' date +"%Y-%m-%d %H:%M:%S %Z")

          # 获取 job 状态
          LINT_STATUS="${{ needs.quick-lint.result }}"

          # 状态图标映射
          case "$LINT_STATUS" in
            success) LINT_ICON="✅" ;;
            failure) LINT_ICON="❌" ;;
            cancelled) LINT_ICON="🚫" ;;
            skipped) LINT_ICON="⏭️" ;;
            *) LINT_ICON="❓" ;;
          esac

          # 生成统一的 summary
          cat >> $GITHUB_STEP_SUMMARY << EOF
          # 🔬 CI 流程报告 - Feature Branch
          ## 📋 基本信息
          - **触发事件**: push
          - **代码仓库**: [${{ github.repository }}](${{ github.server_url }}/${{ github.repository }})
          - **所属分支**: [${{ github.ref_name }}](${{ github.server_url }}/${{ github.repository }}/tree/${{ github.ref_name }})
          - **提交哈希**: [${GITHUB_SHA:0:7}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
          - **提交信息**: ${{ env.COMMIT_MESSAGE }}
          - **应用版本**: ${{ needs.quick-lint.outputs.version }}
          ## 📝 执行状态
          | Job | 状态 |
          |-----|------|
          | Quick lint | $LINT_ICON $LINT_STATUS |
          ## ⏰ 时间戳
          - **完成时间 (UTC)**: $TIMESTAMP_UTC
          - **完成时间 (Asia/Shanghai)**: $TIMESTAMP_CN
          EOF
